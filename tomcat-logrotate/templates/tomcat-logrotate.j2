# {{ ansible_managed }}

{% if 'paths' in item %}
{% for path in item.paths %}
"{{ path }}"
{% endfor %}
{% endif %}
{
  {% if item.options is defined -%}
  {% for option in item.options -%}
  {{ option }}
  {% endfor -%}
  {% endif %}
}